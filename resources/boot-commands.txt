
# sets the display and venv for the terminal to use
if [[ -n "$SSH_CLIENT" ]]; then
    export DISPLAY=$(echo $SSH_CLIENT | awk '{print $1}'):0.0
else
    cd $HOME/4RunnerDash/src
    export DISPLAY=:0

    # configures bluetooth settings
    bluetoothctl power on
    bluetoothctl discoverable-timeout 0
    bluetoothctl discoverable on
    bluetoothctl pairable on

    # runs the program locally
    sudo ../venv/bin/python Audio/BluezAgent.py &
    sudo --preserve-env=TOUCH_SCREEN,DISPLAY ../venv/bin/python main.py
fi
