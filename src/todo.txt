add gps for lat long data in app settings
add dashcam
add analog volume knob
external battery
note I also have an amplifier module if needed

500 gb drive used for map data  add to readme

Nominatim start with (I think):     venv/bin/nominatim query
graphhopper start with:             java -jar Lib/graphhopper.jar server ../resources/graphhopper_config.yml

maybe need nominatim additional files?  https://nominatim.org/downloads/
find out how to import osm.pbf on windows with drive for more speed
make sure servers dont cache data ^^
try just:                           pip install nominatim-db nominatim-api
nominatim on small file
